# yedhu.me

Personal blog. Hand-rolled minimal Jekyll theme — serif body, mono accents, electric blue, auto dark/light with toggle.

## Local development

```sh
bundle install
bundle exec jekyll serve
```

Deployed to GitHub Pages via `.github/workflows/pages.yml` on push to `main`.
